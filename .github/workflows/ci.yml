name: Tests CI

on:    # yamllint disable-line rule:truthy
    workflow_dispatch:
    push:
        branches: ["main"]
    pull_request:

jobs:
    tests:
        name: Run pytests tests
        # SECURITY: secondary gate — keep fork-PR code off self-hosted infra.
        # The primary gate is the repo's fork-PR approval policy ("require
        # approval for all external contributors"): every fork-PR run needs a
        # maintainer click. This condition backstops that human step: an
        # approved fork PR with an unmodified workflow still skips this job.
        # It cannot be the only gate — fork PRs execute their own copy of this
        # file, so a PR that edits this workflow can strip this line. Before
        # approving a fork-PR run, always check the diff for workflow changes.
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
        runs-on: self-hosted

        steps:
            - uses: actions/checkout@v4

            - name: Install uv
              uses: astral-sh/setup-uv@v4
              with:
                  enable-cache: "auto"

            - name: "Set up Python"
              uses: actions/setup-python@v5
              with:
                  python-version-file: ".python-version"

            - name: "Install ffmpeg"
              run: sudo apt-get update && sudo apt-get install -y ffmpeg

            - name: Install the project
              run: uv sync --all-extras --dev

            - name: Display Python version
              run: python -c "import sys; print(sys.version)"

            - name: Checking dependencies
              run: uv run deptry .

            - name: Run Doctests
              run: uv run pytest --doctest-modules alp_data

            - name: Run tests
              run: uv run pytest tests

    test-py-versions-self-hosted:
        name: Run tests on multiple Python versions
        # SECURITY: secondary gate — keep fork-PR code off self-hosted infra.
        # The primary gate is the repo's fork-PR approval policy ("require
        # approval for all external contributors"): every fork-PR run needs a
        # maintainer click. This condition backstops that human step: an
        # approved fork PR with an unmodified workflow still skips this job.
        # It cannot be the only gate — fork PRs execute their own copy of this
        # file, so a PR that edits this workflow can strip this line. Before
        # approving a fork-PR run, always check the diff for workflow changes.
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
        runs-on: self-hosted
        strategy:
            matrix:
                python-version: ["3.11", "3.12", "3.13", "3.14"]

        steps:
            - uses: actions/checkout@v4

            - name: Install uv
              uses: astral-sh/setup-uv@v4
              with:
                  enable-cache: true

            - name: "Set up Python ${{ matrix.python-version }}"
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ matrix.python-version }}

            - name: Install the project
              run: uv sync --all-extras --dev

            - name: Run core tests
              run: uv run pytest tests/io
